import React from "react";

import Layout from '../components/Layout';

const UpcomingEvents = () => {
    return (
        <Layout>
            <div>Upcoming events here</div>
        </Layout>
    );
}

export default UpcomingEvents;